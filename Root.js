import { NavigationContainer } from '@react-navigation/native';
import App from './App';

const Root = () => (
    <NavigationContainer>
        <App />
    </NavigationContainer>
);

export default Root;